# 绯冠书库：UI 设计系统

## 产品原则

网站首先是一座面向陌生读者的世界观 Wiki，其次才是作品展示页。

1. 首页建立气质，正文承担阅读；封面表现不进入条目页。
2. 低饱和绯红用于品牌栏、链接、选中和当前位置；旧金只用于品牌标记、底纹与少量礼仪性细节，不进入正文文字。正文背景使用暖纸灰与暖炭灰中性色。
3. 正文、导航和组件使用无衬线字体；衬线字体只用于“龙冠枯荣”四字。
4. 组件依靠色面、间距和字重建立层级，避免厚边框、大阴影与过度卡片化。
5. 动效只说明状态变化，不模拟火焰、纸页或加载过程。

## 语义颜色

| 角色 | 亮色 | 暗色 | 用途 |
| --- | --- | --- | --- |
| Canvas | `#EDE7E1` | `#292527` | 暖纸灰 / 暖炭灰页面底色 |
| Surface | `#F3EEE9` | `#2F2A2D` | 正文、搜索结果、浮层与抬升表面 |
| Header | `#714048` | `#432C31` | 亮色为暖红酒色，暗色为熏黑红棕 |
| Tabs / Chrome | `#714048` | `#432C31` | 与品牌栏连续的统一色面 |
| On Header | `#F8EEE8` | `#F0E4DF` | 品牌栏文字 |
| Header Gold | `#D0B777` | `#C2A66B` | 品牌图标与顶栏交互图标 |
| Surface Container | `#E6DED8` | `#373033` | 搜索框、低强调组件 |
| Text | `#4A423E` | `#D2C9C5` | 正文 |
| Muted | `#6B605C` | `#ABA2A0` | 次要文字、目录 |
| H1 | `#643940` | `#E7DDD8` | 页面标题；使用中性色，避免品牌色侵入正文 |
| H2 | `#3B3330` | `#EEE7E2` | 主要章节标题 |
| H3 | `#4B413D` | `#D0C6C2` | 次级章节标题 |
| H4 | `#5C504B` | `#BBB1AD` | 低级章节标题 |
| Primary | `#7D454A` | `#D08A85` | 链接与当前项文字 |
| Primary Container | `#F1E2E0` | `#3B2326` | 选中与悬停色面 |
| Ceremonial | `#80612D` | `#C3A56B` | 品牌图标、底纹与礼仪性细节 |
| Focus | `#884244` | `#D08D85` | 键盘焦点和控件状态 |

色卡按 Radix 的 12 阶用途组织：1–2 为背景，3–5 为组件状态，6–8 为边线，9–10 为实色强调，11–12 为文字。正文、次要文字与主色在各自底色上均以 WCAG AA 为最低约束。不要直接在页面中写颜色值；所有组件应引用 `overrides/assets/stylesheets/tokens.css` 的语义变量。

## 字体与阅读宽度

- 正文：约 17px，行高 1.8，每行最多约 50 个汉字；在 44rem 内容栏内仅保留少量呼吸空间，窄屏随容器自然收缩。
- H1：约为正文的 2.1–2.25 倍；H2：约 1.5–1.6 倍；H3：约 1.27 倍；H4：约 1.12 倍。
- H1–H4 使用独立颜色角色；层级不能只依赖颜色，也要同时依赖字号、字重与留白。
- 正文最高字重 600–650，避免大量粗黑文字争抢注意力。
- 作品名使用自托管的 Noto Serif SC 字形子集；正文使用系统无衬线字体栈。

## 组件

- 顶栏：品牌栏和标签栏使用连续的低饱和酒红色面，不使用金色外框、分隔线或当前项下划线。当前标签以暖象牙文字与较高字重标识；开始滚动后标签栏随文档离开视口，仅保留紧凑的品牌、搜索与主题栏。
- 搜索框：收起时在酒红顶栏内使用更深一阶红色容器；展开后与搜索结果一起切换到阅读表面色和正文色，并使用绯红焦点状态。
- 阅读区：画布、正文面和组件面只相差一个暖中性色阶。亮色避免纯白，暗色避免纯黑，通过轻微明度差划分正文与两侧导航。
- 桌面内容轨道：正文色面固定为 `48rem`，左右各预留 `12rem` 导航轨道；顶栏、标签栏与三列正文共享 `72rem` 外壳。页面是否存在侧栏或页内目录，都不改变正文色面的宽度与水平位置。视口不足时再通过断点连续收缩。
- 引用：3px 绯红竖线、统一内边距；署名单独成段并右对齐。
- 提示框：全栏宽、低对比色面、左侧绯红状态线；正文不受阅读宽度二次限制。
- 相关条目：由 front matter 自动生成，默认两列，移动端一列。
- 页内目录：跟随阅读位置，当前项以绯红、字重和左侧标记共同表达；每级缩进只增加约 12–16px，避免长中文标题过早换行。
- 首页入口：只保留一个“开始阅读”，使用文字链接与短下划线状态，不使用矩形按钮或箭头。
- 背景标志：`ornament-vector.svg` 作为 CSS mask 固定在完整内容视口的几何中心，页头与页尾不参与位置补偿；宽屏尺寸随视口在 420–600px 间变化，亮色 4.2%、暗色 4.5% 不透明度，位于正文下方且不参与排版和交互。

## 动效

| 场景 | 时长 | 表现 |
| --- | ---: | --- |
| 悬停与聚焦 | 140ms | 颜色、底线、轻微色面变化 |
| 条目进入 | 200ms | 透明度与 2px 位移 |
| 首页首次出现 | 220ms | 透明度与 4px 位移 |
| 纹章星尘场 | 持续、极慢 | 微尘、细长光屑与少量 `❀` 缓慢漂移；正文区域自动减弱，不响应鼠标 |

所有动效使用标准链接并渐进增强；`prefers-reduced-motion: reduce` 时关闭。纹章星尘场使用独立 Canvas，宽屏保持 32–46 个细小元素，按 70% 微尘、20% 细长光屑、10% `❀` 字符分配；正文范围降至 20% 强度，渲染限制在 30 FPS，页面隐藏时暂停。`❀` 由浏览器字体系统绘制，并使用低速旋转和曲线漂移。背景层始终使用 `pointer-events: none`，页面不得因动效延迟真实导航。

## 内容与呈现边界

- `docs` 只存 Markdown、内容图片、模板与编辑配置。
- `overrides` 存 Jinja 模板、CSS、JavaScript、主题字体。
- `tools` 存导航、索引、关联条目和验证逻辑。
- 正文不写按钮、卡片、导航等 UI HTML。
- 段落后使用列表时必须先留一个空行，保证 Obsidian 与 Python-Markdown/MkDocs 都生成语义化列表。

主要实现依据：[Material Design 3](https://m3.material.io/)、[Material for MkDocs 元数据继承](https://squidfunk.github.io/mkdocs-material/plugins/meta/)、[Awesome Nav](https://lukasgeiter.github.io/mkdocs-awesome-nav/)、[Obsidian Templates](https://obsidian.md/help/plugins/templates) 与 [Obsidian Bases](https://obsidian.md/help/bases/syntax)。
