# 阅读与页面转场：产品设计

## 设计目标

《龙冠枯荣》首先是一座供陌生读者持续查阅的世界观 Wiki，而不是动效展示页。交互需要同时满足四个目标：

- 首页建立作品气质，但入口必须清楚、克制；
- 条目之间的跳转连续而不迟滞；
- 不接管链接、浏览器历史或 MkDocs Material 的页面生命周期；
- 在不支持动效或开启“减少动态效果”时，导航仍保持完整可用。

首页因此不再使用矩形 CTA 和模拟翻页。主入口改为无底色、无边框的文字链接“开始阅读”，次入口为“条目索引”；悬停只改变文字颜色与细下划线，不再附加方向箭头。

## 技术选型

| 方案 | 优势 | 本项目中的代价 | 结论 |
| --- | --- | --- | --- |
| [跨文档 View Transitions](https://developer.mozilla.org/en-US/docs/Web/API/View_Transition_API/Using#cross-document_mpa_transitions) | 浏览器原生、零依赖，不需要脚本拦截链接 | 需要渐进增强，旧浏览器会直接跳转 | **采用** |
| [Swup](https://github.com/swup/swup) | 缓存、历史与过渡插件完整 | 接管页面替换，需要协调 Material 初始化与搜索 | 不引入 |
| [Barba.js](https://barba.js.org/docs/getstarted/intro/) | 适合复杂 SPA 式转场 | 对文档站过重，同样改变页面生命周期 | 不引入 |
| [StPageFlip](https://github.com/Nodlik/StPageFlip) | 成熟的电子书翻页效果 | 面向多页书籍，会克隆并管理页面，不适合 Wiki 导航 | 不引入 |

全站通过 `@view-transition { navigation: auto; }` 为同源跨文档导航增加原生过渡。支持该能力的浏览器播放短暂淡出与淡入；不支持时继续执行普通链接导航。页面跳转没有 JavaScript 计时器、锁定状态或人为等待。

## 动效规格

| 场景 | 时长 | 表现 |
| --- | ---: | --- |
| 首页内容首次出现 | 180 ms | 透明度 0→1，并向上移动 4px |
| 顶部站点框架 | 全程稳定 | 顶栏与标签不随正文闪烁或位移 |
| 当前条目离开 | 110 ms | 只做轻微淡出 |
| 新条目进入 | 180 ms | 淡入，并从下方移动 2px |
| 首页入口悬停 | 160–180 ms | 下划线从中心展开 |

一次站内换页的视觉收尾不超过约 180ms。动效用于说明“内容已经切换”，不模拟真实纸张，不加入旋转、火焰、音效或加载遮罩。

## 阅读层级

- 正文基准约为 16.4px / 1.76 行高，字重 400，每行约 38 个汉字。
- H1 约 35px、H2 约 24px，标题和强调文字最高使用 600 字重。
- “开始阅读”是导览页，只保留顶部全站导航；左右侧栏和面包屑不重复占位。
- 分类页保留左侧结构导航；长条目保留自动跟随的右侧目录。
- 浅色模式沿用暖纸色，暗色模式使用低眩光的暖灰黑，并分别校准正文、标题、次要文字和链接的对比度。

## Wiki 导航

- MkDocs Material 的 `toc.follow` 负责让右侧页内目录跟随当前阅读章节，不额外编写 ScrollSpy。
- 当前目录项通过文字权重和淡金左侧标记共同强调，避免只依赖颜色。
- 面包屑用于说明条目在世界观层级中的位置；条目索引用于穷举查找，二者不承担同一种任务。
- 站内锚点使用浏览器原生平滑滚动，并为固定顶栏预留滚动偏移。

## 首屏资源策略

- 四张高分辨率原图继续留在仓库中作为母版，但不进入发布目录。
- 桌面端按照有效像素宽度选择 1600px 或 2560px WebP；移动端使用以主要人物为焦点的 960×1280 WebP。
- 页面只预加载当前时段和当前设备对应的一张图片，JavaScript 初始化沿用同一 URL，避免重复请求。
- 社交分享图使用 1200×630 JPEG，保留原 PNG 作为母版但不发布。

## 行为边界

- 所有入口仍是标准 `<a>` 链接，支持新标签页、复制地址、浏览器前进与后退。
- 站内锚点和外部链接不需要自定义拦截；锚点平滑滚动由 CSS 渐进增强。
- 用户开启 [`prefers-reduced-motion`](https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-reduced-motion) 时关闭页面转场和首页位移动画。
- 无 JavaScript 时首页入口与 Wiki 导航照常工作。
- 动画不请求额外资源，不引入 CDN 或第三方运行时。

## 验收标准

- 点击后立即开始真实导航，不存在 500ms 以上的脚本等待。
- 首页在正常桌面与移动视口保持一屏，无横向或纵向溢出。
- 站内条目、目录入口和上一篇/下一篇使用同一套渐进式转场。
- 开启减少动态效果后不播放透明度或空间运动。
- 浅色与暗色的正文、次要文字和小号目录文字满足可读对比度。
- 首页首次访问只请求一张约 110–315KB 的设备匹配封面，不再发布 2MB 以上的原图。
- MkDocs 中文、英文、日文配置均可严格构建。
