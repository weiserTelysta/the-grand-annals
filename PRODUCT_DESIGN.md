# 首页进入仪式：产品与交互设计

## 设计目标

首页进入效果不是加载动画，而是读者第一次主动“打开世界”的仪式。它需要同时满足三个目标：

- 强化《龙冠枯荣》作为幻想世界档案的辨识度；
- 让“翻开《龙冠枯荣》”的文案与实际操作一致；
- 不接管网站导航，不牺牲搜索、浏览器历史、移动端与无障碍体验。

最终采用“暗金档案翻页”：点击主入口后，首页作为档案封页向左翻开，自由边短暂出现暗红金灼痕，底层露出“卷一／档案启封”，随后进入“开始阅读”。火焰只作为边缘意象，不使用粒子或整页燃烧。

## 技术选型

| 方案 | 成熟度与优势 | 在本项目中的问题 | 结论 |
| --- | --- | --- | --- |
| 原生 CSS 3D、Animation 与链接导航 | Web 标准；零依赖；不改变 MkDocs 页面生命周期 | 需要自行完成时序与状态保护 | **采用** |
| [跨文档 View Transitions](https://developer.chrome.com/docs/web-platform/view-transitions/cross-document) | 浏览器原生页面快照与过渡 | 浏览器支持仍不完全一致，不能作为唯一入口保障 | 保留为未来增强 |
| [Swup](https://github.com/swup/swup) | 活跃、模块化，包含缓存、历史和无障碍插件 | 会替换页面容器并接管加载周期，需额外协调 Material 初始化 | 不引入 |
| [Barba.js](https://barba.js.org/docs/getstarted/intro/) | 体积较小，适合定制 SPA 式转场 | 同样需要接管页面与第三方脚本生命周期 | 不引入 |
| [StPageFlip](https://github.com/Nodlik/StPageFlip) | 成熟的书籍翻页库，支持 HTML 与移动端 | 面向多页电子书，会克隆和管理页面；对一次性入口过重 | 不引入 |

选择成熟技术不等于必须增加依赖。这里使用被浏览器长期支持的 CSS 3D 与原生导航，把复杂度控制在与产品价值相匹配的范围内。

## 交互时序

| 时间 | 状态 | 读者感知 |
| --- | --- | --- |
| 0 ms | 点击确认 | CTA 锁定，防止重复触发 |
| 0–170 ms | 纸页起势 | 封页轻微抬起，暗金边出现 |
| 170–650 ms | 主翻页 | 首页向左收拢，底层档案纸逐渐露出 |
| 260–940 ms | 档案显现 | “卷一／档案启封”淡入 |
| 约 940 ms | 导航 | 打开“开始阅读”页面 |
| 1150 ms | 安全兜底 | 动画事件异常时仍保证完成导航 |

## 行为边界

- 只在首页主 CTA 的普通左键或键盘激活时播放。
- `Ctrl`、`Cmd`、`Shift` 点击和“新标签打开”保持浏览器原生行为。
- 不支持 3D 变换的浏览器直接打开链接。
- 用户开启“减少动态效果”时直接打开链接；实现遵循 [`prefers-reduced-motion`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)。
- 返回首页或从浏览器缓存恢复时，动画状态、忙碌状态和 CTA 锁定自动清除。
- 动画不请求新图片、不引入 CDN、不增加第三方 JavaScript。

## 验收标准

- 动画总等待不超过 1.15 秒。
- 动画失败不妨碍导航。
- 桌面端和 390px 移动端无横向滚动。
- 快速重复点击只发生一次导航。
- 返回首页后 CTA 可以再次使用。
- 减少动态效果模式不播放整页空间运动。
- MkDocs 严格构建与站内链接检查继续通过。

## 后续评估

当 Firefox 等目标浏览器对跨文档 View Transitions 的支持稳定后，可以把当前底层档案画面替换成目标页面的真实浏览器快照。当前实现已达到进入仪式的产品目标，不应继续堆叠火焰粒子、音效或长时间加载动画。
